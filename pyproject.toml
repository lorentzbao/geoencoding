[project]
name = "japanese-geocoding"
version = "0.1.0"
description = "Japanese Address Geocoding using ZENRIN Maps API"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
